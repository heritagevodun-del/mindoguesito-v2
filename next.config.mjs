/** @type {import('next').NextConfig} */
const nextConfig = {
  // Les boucliers sont levés.
  // Next.js bloquera la construction s'il détecte des erreurs de code critiques.
  // C'est la configuration recommandée pour la production.
};

export default nextConfig;
